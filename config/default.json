{
    "port": 3040,
    "database": {
        "db": "2Q2R-server",
        "username": null,
        "password": null,
        "options": {
            "logging": true,
            "dialect": "sqlite",
            "storage": "store.db"
        }
    },
    // This part needs to be owerwritten in local.json
    "serverAuth": "token",
    "authTokens": {
        // map: token => name
        // appID = sha256(name)
        "fake_12345": {
            "appID": "fake_12345678",
            "name": "2Q2R Server Demo"
        }
    },
    "fbServerKey": "This_is_not_a_valid_token"
}